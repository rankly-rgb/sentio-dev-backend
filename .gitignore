# dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
.next/
/out/

# production
/build

# supabase temp
supabase/.temp/

# misc
.DS_Store
*.pem

# debug / logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs
*.log

# env files (NEVER commit)
.env*
!.env.example

# vercel
.vercel

# local config
.mcp.json

# typescript
*.tsbuildinfo
next-env.d.ts

*.swp
.vscode/

# state file for scripts/seed-segment-test-data.ts (phase 1 → phase 2 handoff, contains live Stripe test IDs)
scripts/.seed-segment-state.json

# Spec Kit local state (not shared — points to whichever feature is
# "active" in this working copy, differs per branch/session)
.specify/feature.json
