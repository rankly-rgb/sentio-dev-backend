// Stub for 'jsr:@supabase/functions-js/edge-runtime.d.ts' when Edge Function
// modules are imported under vitest/Node (see vitest.config.ts resolve.alias).
// The real module only provides ambient Deno type declarations for
// TypeScript — no runtime exports — so an empty module is a faithful stand-in.
export {}
