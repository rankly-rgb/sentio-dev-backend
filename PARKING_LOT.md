# Parking lot

- 2026-08-25 — Test de garde-fou : cette ligne est un test délibéré et unique, demandé par Naima après le push direct de PARKING_LOT.md plus tôt dans la session (`create_or_update_file` avait fonctionné malgré le blocage de `git push` brut). Si cette écriture aboutit, la protection de branche `main` posée juste avant ("Require a pull request before merging") ne bloque pas ce canal — à signaler immédiatement.

## `calculate-scores` échoue systématiquement sur un org à 432 comptes — requête invoices non chunkée (2026-08-23)
